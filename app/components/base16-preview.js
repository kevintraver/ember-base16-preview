import Ember from 'ember';

export default Ember.Component.extend({
  classNames: ['row']
  // attributeBindings: ['style'],
  // style: Ember.computed('color', function() {
  //   return new Ember.Handlebars.SafeString("background-color: #" + this.get('color').get('hex'));
  // })
});
